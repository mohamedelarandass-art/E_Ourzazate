/**
 * Context Index (Barrel Export)
 * 
 * Central export point for all context providers.
 * 
 * @module context
 */

export { ThemeProvider, useThemeContext } from './ThemeProvider';
export type { Theme, ResolvedTheme } from './ThemeProvider';
